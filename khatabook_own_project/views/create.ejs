<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Khaatabook</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #fafafa;
            margin: 0;
            padding: 0;
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            padding: 24px;
        }
        .navbar-left {
            font-weight: bold;
        }
        .navbar-right a {
            text-decoration: none;
            color: #222;
            margin-left: 24px;
        }
        .container {
            max-width: 500px;
            margin: 40px auto;
            background: #fff;
            padding: 40px;
            border-radius: 6px;
            box-shadow: 0 4px 16px rgba(0,0,0,0.04);
        }
        h3 {
            font-size: 18px;
            margin-bottom: 18px;
        }
        input[type="text"] {
            width: 100%;
            padding: 10px;
            border-radius: 4px;
            border: 1px solid #eee;
            margin-bottom: 18px;
            font-size: 15px;
            background: #fafafa;
        }
        textarea {
            width: 100%;
            height: 120px;
            padding: 10px;
            border-radius: 4px;
            border: 1px solid #eee;
            font-size: 15px;
            background: #fafafa;
            margin-bottom: 22px;
            resize: none;
        }
        button {
            background: #007aff;
            color: #fff;
            padding: 10px 22px;
            border: none;
            border-radius: 4px;
            font-size: 15px;
            cursor: pointer;
        }
        button:hover {
            background: #005bb5;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <div class="navbar-left">Todo App <div>
        <div class="navbar-right">
            <a href="/">Home</a>
            <a href="/create">Create New Todos</a>
        </div>
    </div>
    <div class="container">
        <h3>Create New Todo</h3>
        <form action="/createtodo" method="post">
            <input type="text" name="title" placeholder="Enter your Todos..." />
            <textarea name="content" placeholder="plan your day...."></textarea>
            <button type="submit">Create New Todo</button>
        </form>
    </div>
</body>
</html>
