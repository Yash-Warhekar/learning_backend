<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Khaatabook</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #fafafa;
            margin: 0;
            padding: 0;
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            padding: 24px;
        }
        .navbar-left {
            font-weight: bold;
        }
        .navbar-right a {
            text-decoration: none;
            color: #222;
            margin-left: 24px;
        }
        .container {
            max-width: 600px;
            margin: 30px auto;
            background: #fff;
            padding: 30px 50px 40px 20px;
            border-radius: 6px;
            box-shadow: none;
        }
        .info-text {
            font-size: 16px;
            margin-bottom: 8px;
        }
        .info-text a {
            color: #007aff;
            text-decoration: underline;
        }
        .hisaab-details {
            font-size: 14px;
            color: #222;
            margin-bottom: 22px;
        }
        button {
            background: #efefef;
            color: #aaa;
            padding: 9px 22px;
            border: none;
            border-radius: 4px;
            font-size: 15px;
            cursor: not-allowed;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <div class="navbar-left">Todo App</div>
        <div class="navbar-right">
            <a href="/">Home</a>
            <a href="create">Create New Todos</a>
        </div>
    </div>
    <div class="container">
        <div class="info-text">
            Todo Title:-
            <a href="#"><%=filename%></a>
            date
        </div>
        <div style="height: 50%; width: 40%;" class="hisaab-details">
            <%=data%>
        </div>
        <button disabled>Edit Hisaab</button>
    </div>
</body>
</html>
